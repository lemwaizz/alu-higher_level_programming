#!/usr/bin/python3
"""function that checks if object is instance of a given class
"""


def is_same_class(obj, a_class):
    """checks if obj is an instance of class
    """
    if obj isinstance(obj, a_class):
        return True
    else:
        return False
